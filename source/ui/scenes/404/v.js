module .exports = design =>
	so ((_=_=>
	view_of (design),
	where
	, view_of = _design =>
		
	, iface = 
	, {[ T (iface (design)
		) (panic_on ([ [x => ! x, 'interface not matching'] ])) ]:$1}
	)=>_)
