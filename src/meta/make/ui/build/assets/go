#!/usr/bin/env bash
use_deps || {
	echo "please compile $0"
	exit 1
} && use_node || {
	exit 1
} && use_file "$src" || {
	exit 1
} && node "fn.js" || {
	echo "failed to make build ui/assets"
	exit 1; }
